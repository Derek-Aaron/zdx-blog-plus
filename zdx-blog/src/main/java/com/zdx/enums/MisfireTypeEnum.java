package com.zdx.enums;

public enum MisfireTypeEnum {
    /**
     * 默认
     */
    DEFAULT,
    /**
     * 立即触发
     */
    IMMEDIATELY,
    /**
     *触发一次
     */
    ONCE,
    /**
     * 不触发执行
     */
    NOTTRIGGER
}
